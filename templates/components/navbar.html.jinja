<style>
    @import url('https://fonts.googleapis.com/css2?family=Fruktur&display=swap');

    .fruktur-regular {
        font-family: "Fruktur", system-ui;
        font-weight: 400;
        font-style: normal;
    }
</style>
<nav class="flex p-4 m-8 shadow-black shadow-md justify-between ">

    <a href="/" class="rounded text-red-600 text-center item-center pt-1 text-shadow-md text-4xl fruktur-regular">Riffts</a>
    <div class=" flex gap-2">
        <a href="/browse" class="bg-stone-400 rounded text-white text-center p-2 pt-2">Browse</a>
        {% if not current_user.is_authenticated%}
        <a href="/login" class="p-2 bg-stone-400 rounded text-white  ">Login</a>
        {%else%}
        <div>
            <button class="bg-stone-400 rounded text-white text-center p-2 group relative ">
                Hi, {{current_user.name}}
                <div
                    class=" flex flex-col justify-center gap-2 item-center bg-gray-200 border border-gray-400 p-2 text-gray-400 text-shadow-lg absolute top-full right-0 hidden group-hover:block ">
                    <a href="/logout" class="   hover:border-2">Logout</a>
                    <a href="#" class="  hover:border-2">Settings</a>
                    <a href="/cart" class="  hover:border-2">Cart</a>
                </div>
            </button>
        </div>
        {%endif%}

    </div>
</nav>