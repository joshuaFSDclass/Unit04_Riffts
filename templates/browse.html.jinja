<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Page Title</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>

</head>
<body class = "bg-gray-200">
{% include "components/navbar.html.jinja" %}
 <h1 class= "text-6xl text-red-600 justify-start mr-40 p-4">
 BROWSE PRODUCTS
 </h1>
<main class =" grid grid-cols-3 gap-2 ml-3 mt-10 bg-gray-300">
    {% for x in products %}
        <a href="/product/{{ x['ID']}}" class="grid text-center shadow-md">
            <img class="h-50 mx-auto mt-8" src="{{ x['Image'] }}"> <br>
            {{x ["Name"]}}<br>
            ${{x ["Price"]}}<br>
            {{x["Brand"]}}<br>
            {{x["Size"]}}<br>
            {{x["Category"]}}
        </a>
    {%endfor%}
</main>
 

 
</body>
</html>